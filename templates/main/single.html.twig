{% extends "base.html.twig" %}

{% block title %}{{ movie.title }}{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1>{{ movie.title }}</h1>
        </div>
        <div class="row">
        
        <div class="col-6">
        
        {% for actor in movie.actors %}
            <li>{{ actor.role }} <em>(joué par : {{ actor.person.name }})</em></li>
        {% endfor %}
        </div>
        <div class="col-6">
        
            {% for genre in movie.genres %}
                <li>{{ genre.name }}</li>
            {% endfor %}
        
        </div>
        </div>
    </div>

{% endblock %}