{% extends "admin/base.html.twig" %}

{% block body %}

	<div class="container">
		<div class="row">
			<h1>Ajouter le job</h1>
		</div>
		<div class="row">
			{{ form_start(form) }}
            {{ form_row(form.name) }}
			{{ form_row(form.department) }}
			<button>Ajouter</button>
			{{ form_end(form) }}
		</div>
	</div>

{% endblock %}